//
// Created by Flash on 2/4/2024.
//

#include "Scripting.h"

/*
int Scripting::print(lua_State* L)
{
    /*const char* sender = lua_tostring(L, 1);

    if (sender == nullptr)
    {
        Logger::Write("Scripting", "Invalid sender", Logger::Error);
        return 0;
    }

    const char* text = lua_tostring(L, 2);

    if (text == nullptr)
    {
        Logger::Write("Scripting", "Invalid text", Logger::Error);
        return 0;
    }

    int level = lua_tointeger(L, 3);

    Logger::Write(sender, text, Logger::Info);#1#
    return 1;
}
*/

